[pytest]
testpaths = tests
# Streaming is handled by conftest.py (xdist-safe, controller-only). DISABLE the
# bundled zero-code plugin so it doesn't open a second, double-recording session.
addopts = -p no:testlookup
