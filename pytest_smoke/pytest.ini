[pytest]
# Mirror of testng_smoke's testng.xml <test> block: the two modules below feed
# one live session. The testlookup-reporter plugin is auto-loaded via its
# pytest11 entry point — no conftest wiring needed (the zero-code path).
testpaths = tests
